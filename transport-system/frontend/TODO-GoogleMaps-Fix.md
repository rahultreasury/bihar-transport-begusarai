# TODO-GoogleMaps-Fix

- [ ] Update Home.jsx to prevent blank page when Google Maps API key is missing or script fails
- [x] Replace brittle drop autocomplete DOM query with proper Autocomplete wiring for both pickup and drop

- [ ] Add loading/error UI + fallback text inputs (no crash)
- [ ] Store selected pickup/drop text, place_id, and lat/lng in React state
- [ ] Ensure Vehicle dropdown + Check Price button still work
- [ ] Ensure autocomplete suggestions show while typing
- [ ] Run `npm run dev` and verify Home renders (no blank page)
- [ ] Run `npm run build` to ensure compile success

